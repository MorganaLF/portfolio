include ../subtitle-group/subtitle-group

mixin profile({ titleOptions, text, importantText })
  section.profile
    +subtitle-group(titleOptions)
    p.profile__text= text
      span.profile__important-text= importantText