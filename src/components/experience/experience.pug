include ../section-header/section-header
include ../unordered-list/unordered-list

mixin experience({ titleOptions, unorderedListOptions })
  section.experience
    +section-header(titleOptions)
    .experience__content
      +unordered-list(unorderedListOptions)