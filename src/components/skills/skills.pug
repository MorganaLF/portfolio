include ../subtitle-group/subtitle-group
include ../progress-bar/progress-bar

mixin skills({ titleOptions, progressBarOptions })
  section.skills
    +subtitle-group(titleOptions)
    .skills__progress-bar-container
      +progress-bar(progressBarOptions)
